#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

using namespace std;

int main()
{
    // vector<int> A;
    // for (int i = 0; i < 5; i++)
    //     A.push_back(i);
    
    // for (int i = 0; i < A.size(); i++)
    //     cout << A[i] << " ";
    // cout << endl;
    // cout << A.size() << endl;
    // A.erase(A.begin(), A.begin() + 2);
    // cout << A.size() << endl;
    // for (int i = 0; i < A.size(); i++)
    //     cout << A[i] << " ";
    // cout << endl;

    // string s = "123";
    // s = s[1];
    // int num = atoi(s.c_str());
    // cout << num << endl;
    // cout << num + 1 << endl;

    int time;
    cin >> time;
    cout << time << endl;
    
}